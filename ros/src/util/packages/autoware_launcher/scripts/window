#! /usr/bin/env python

import sys
from autoware_launcher import AwApplication
from autoware_launcher import AwMainWidget

application = AwApplication(sys.argv)
widget = AwMainWidget()
widget.show()
sys.exit(application.exec_())

