<launch>

    <arg name="ROOT_PATH" default="$(env PWD)"/>

    <arg name="config" value="root/rviz/default.rviz"/>
    <node name="$(anon rviz)" pkg="rviz" type="rviz" args="-d $(arg ROOT_PATH)/$(arg config)"/>

</launch>
